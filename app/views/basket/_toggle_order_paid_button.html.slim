a href="#{toggle_paid_order_path(@order.uuid)}" title="Click, if you want to #{@order.paid? ? "un" : ""}mark your order as paid." data-method="put" data-remote="true"  data-auto-reload="true" class="button #{@order.paid? ? 'warning' : 'success'}"
  '  Mark Order as #{@order.paid? ? "NOT" : ""} Paid

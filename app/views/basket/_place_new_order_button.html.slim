- confirm = @order ? 'There’s already an order for you. Continue only if this is for someone else (=OK), otherwise edit your old order (=Cancel).' : ''
- color = @order ? 'primary' : 'success'
a href="#{new_order_path}" title="This initiates a blank order. You can order everything which you can order normally on pizza.de." class="button #{color}" data-confirm="#{confirm}" data-no-turbolink="" ✚ Place New Order

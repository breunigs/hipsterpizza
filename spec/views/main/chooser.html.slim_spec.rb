require 'spec_helper'

RSpec.describe 'main/chooser', type: :view do
  it 'shows multiple provider buttons'

  context 'single basket mode' do
    it 'shows multiple providers if no open basket'

    it 'does not display multiple providers if open basket available'
  end
end
